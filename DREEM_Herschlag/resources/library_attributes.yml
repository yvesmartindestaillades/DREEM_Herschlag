mandatory:
  - name # must be the same as in the ref_fasta
optional:
  - flank
  - ROI_start  # 0-indexed
  - ROI_stop   # 0-index
  - sub-library
  - structure
  - base_pairing_prob
  - deltaG_ROI
  - deltaG